# AI 交接文档 —— 《电子斗蛐蛐》桌面宠物游戏

> 本文档写给即将接管本项目的 AI（或人类）。读完这一份，你应该能独立继续开发。
> 最后更新：2026-09-20，HEAD = `4cb5a3d`

---

## 0. 一句话概述

**电子斗蛐蛐**：PySide6 桌面宠物游戏。养一只蛐蛐挂在桌面上（打字/点击吃经验升级），
蛐蛐能自动挑战**桌面副本**（10 难度 × 20 层连战，直接在桌面上打，无窗口），
掉落装备（6 部位 × 7 品质 × 随机词条），穿戴提升属性，挑战更高难度。
数值 100% 由 Excel 配置表驱动。仓库：`github.com/Wx0823/QuQuBattleGame`。

## 1. 运行环境与入口

- **Python 3.13** + **PySide6**（无 requirements.txt，依赖仅 PySide6 + openpyxl + opencv-python/numpy，后两者只有 checklist 用）
- 无构建系统：直接 `python main.py`
- 仓库根：`D:\Projects\QuQuBattleGame`
- **验证一切的唯一手段**：`python battle_checklist.py`（23 项自检，约 5 分钟）。任何改动后必须全量跑过再交付。

## 2. 文件结构（职责边界严格，改动前先看这张表）

| 文件 | 职责 | 依赖约束 |
|---|---|---|
| `main.py` | 桌宠本体窗口（Pet 类）：挂机绘制、模式切换按钮、拖拽、托盘、存档 | 依赖全部 |
| `stage.py` | **BattleStage 战斗舞台**：战场逻辑+走位编排+绘制，纯逻辑+QPainter | 不依赖 main/arena |
| `battle.py` | 战斗引擎：Tick 推进、伤害/士气/克制/状态公式，事件流 `step()` | 纯逻辑，不知道 UI |
| `dungeon.py` | 副本调度器：敌人生成/结算/断点存档（DungeonRun） | 纯逻辑 |
| `equipment.py` | 装备：生成/掉落/背包/属性加成（equipment 模块） | 纯逻辑 |
| `cricket.py` | 蛐蛐绘制（正/侧视、步态、振翅）+ palette | 只管画 |
| `cricket_art.py` / `art_assets.py` | 手绘贴图与可动关节、图集缓存及缩小采样 | 只管画 |
| `assets/art/` | 双视角品种图集、装备、特效、陶盆和 manifest | 美术资源 |
| `stats.py` | StatsDB：读 data/stats.json，compute() 算属性 | 只读表 |
| `arena.py` | 竞技场窗口（好友对战宿主）+ DungeonSelect 难度面板 | 薄壳，逻辑在 stage |
| `equipment_ui.py` | 属性/装备面板（格子背包 + 槽位 + 词条详情） | Qt UI |
| `equipment_viewmodel.py` | 装备面板状态、背包快照、筛选排序、换装差值 | 不依赖 Qt |
| `equipment_widgets.py` | 装备图标、深色滚动区、池化背包格子 | Qt UI |
| `equipment_ui_checklist.py` | 装备面板离屏回归（临时背包存档） | 验证 |
| `panel.py` / `settings.py` | 卡片面板基类 / 蛐蛐属性面板 / 设置面板 | UI |
| `battle_checklist.py` | 23 项自检（公式/引擎/玩法/表现/副本模拟） | 验证 |
| `生成数值表.py` / `导出数值表.py` | Excel 生成 / Excel→JSON 导出 | 管线 |
| `数值表/QuQu数值表.xlsx` | **唯一数值真相源**（12 张 sheet） | 策划直接编辑 |
| `data/*.json` | 运行时持久化：`stats.json`(表导出)、`save.json`(蛐蛐)、`dungeon.json`(副本断点+通关)、`inventory.json`(装备)、`settings.json` | |

## 3. 数据管线（最重要的约定）

```
数值表/QuQu数值表.xlsx  ──(导出数值表.py / 启动时自动比对 mtime)──▶  data/stats.json ──▶ StatsDB
```

- **策划（用户）直接改 Excel，程序不写 Excel**；`生成数值表.py` 只用于重建表结构（会覆盖手改内容，谨慎运行）
- 12 张 sheet：1 属性定义 / 2 品种 / 3 成长(80级) / 4 招式 / 5 克制 / 6 状态 / 7 战斗常数 / 8 天赋 / 9 副本难度 / 10 装备部位 / 11 装备品质 / 12 装备词条
- **动态扩展是硬需求**：部位、品质、词条、难度加行即生效，代码里不许硬编码数量
- 表头名不能改；ID 列是程序索引

## 4. 核心架构与关键机制

### 4.1 战斗引擎（battle.py）
- `Battle(db, f0, f1, seed)` → 循环 `step()` 吐事件 dict → **表现层播放事件**（stage._play）
- **同种子 = 同战斗**（回放/测试/断点都靠这个）
- 战斗体 Fighter 开打时快照属性 → **战斗中改装备/升级不影响当前场，下一场生效**（这是设计，不是 bug）
- 士气损失按 `100/受击方最大血量` 归一（否则高等级 3 击崩溃）；成长表 41 级后 HP+30/ATK+2/SPD+0（保证战斗时长不随等级缩水）—— **这两条是模拟器跑出来的血泪教训，别改回去**

### 4.2 战斗舞台（stage.py）
- `BattleStage` 被**两个宿主**共用：竞技场窗口（style='arena'，带陶罐+角标 HUD）/ 桌宠桌面模式（style='desktop'，透明战场+悬浮血条+进度徽章）
- 事件播放把伤害事件转成冲刺/击退/尘土/角力（grapple）/KO 掀翻动画
- 宿主职责：起 QTimer 调 `stage.update(dt)` + `paintEvent` 里调 `stage.draw(p)` + 接回调（`on_diff_clear` / `on_pvp_end`）

### 4.3 桌面副本（main.py + dungeon.py + stage）
- 模式切换：挂机（蛐蛐形态）⇄ 副本（窗口临时扩为 660×440 透明战场，`clearMask()` 解除蛐蛐 mask）
- 【副本】按钮一键直进：**断点优先**，否则打"已解锁且未通关"的最前沿难度（`_quick_enter_dungeon`）
- 【桌面】按钮 / 右键菜单：`end_desktop_battle()`（存断点、恢复蛐蛐尺寸/mask/位置）
- 通关流程：20/20 打完 → `on_diff_clear` 回调 → 三连金色飘字（副本通关/首通/已解锁「下一档」）→ 4.5s 后自动撤回挂机
- **血量规则（策划拍板，别改）**：跨场结转（胜 +50%HP/+30%STA，清层回满，败满血退上一层——新手第 1 层失败本层重来）；失败给 60% 参与奖经验（卡关挂机刷级生态）
- 难度曲线实测（sim）：新手 19min → 深渊 283min（2026-09-24 修正战斗规则后的无装备模拟；旧记录 111min 已过时）；**绝望=长线墙（最远 19 层）、真神=终极天花板**（80 级属性封顶，通关需场外成长）—— 这是 4h/日一月生态的设计，不是 bug

### 4.4 装备（equipment.py + equipment_ui.py）
- 词条数值 = 基础值 × 品质数值倍率 × 浮动(0.85~1.25)，3% 概率按高 1 档品质生成
- 玩家最终属性 = `equipment.effective_stats()`（基础 compute + 穿戴词条合计）；战斗体通过 `make_fighter(..., extra_stats=)` 注入
- 背包：格子池复用模式（**不要** deleteLater+重建滚动区内容，有渲染时序坑），1s QTimer 轮询快照自动刷新（战斗掉落 1s 内进格）
- 黑金（q07）：暗色品质用亮度自适应显示金色（`_ui_color`），未来暗色品质自动继承

### 4.5 UI 约定
- 按钮必须并入窗口 **mask**（`_update_mask` unite 按钮 geometry）否则不渲染不可点
- `show()` 父窗口会连带显示未显式 hide 的子部件 —— 动态面板创建后必须显式 hide
- QSS **不支持 8 位 hex**（#RRGGBBAA 会被错误解析）；滚动区视口默认浅灰底，必须显式设深色

### 4.6 装备 UI 框架重构（2026-09-24）

- `EquipmentPanel(pet)` / `refresh()` / `show_near(anchor)` 保持原宿主接口；三栏分别为属性穿戴、背包、详情操作。窗口默认 960×640，显示位置限制在所在屏幕可用区域。
- `EquipmentViewModel` 每次刷新只读取一次完整背包，快照同时包含品种、等级、天赋与战斗状态；同数量装备内容变更也会刷新。穿戴动作执行前再次读档校验选择。
- 部位和品质筛选来自配置顺序，支持最新/品质排序；替换预览同时显示获得和失去的属性。选中状态独立于筛选，筛选隐藏装备时详情仍保留当前选择。
- `InventoryGrid` 复用格子池，空格禁用，已穿戴装备显示金色角标；槽位与详情使用独立滚动区，新增部位或长词条不会挤出操作按钮。
- UI 回归：`python equipment_ui_checklist.py`，覆盖空背包、穿卸替换、同数量词条变化、升级刷新、配置扩展、格子池及深色像素验证。使用临时背包，不改玩家存档。
- 全量 `battle_checklist.py` 现已在命令行入口使用 `test_support.isolated_game_data()` 隔离存档；可直接运行。其他宿主直接调用测试函数时仍需隔离。
- 本机 Python 不在 PATH；可使用 `启动.bat` 中 `pythonw.exe` 同目录的 `python.exe` 运行验证。

## 5. 测试与验证（battle_checklist.py）

### 2026-09-24 美术替换

- 已接入 6 品种侧面/俯视手绘躯干，六类装备部位贴图、四类战斗特效、竞技场陶盆；桌面副本保持透明。
- `palette_from_hex(color, species_id)` 增加可选品种 ID。主程序和敌人生成已传 ID；新宿主也应传 ID，未映射品种保持原矢量回退。
- `cricket_art.py` 分层绘制躯干、腿与触须，保留呼吸/转身/跳跃/步态/振翅/KO；主视图、头像、战斗共用入口。
- `assets/art/manifest.json` 映射资源，不改变 Excel 数值；图集切片、透明裁边和缩小缓存集中在 `art_assets.py`。完整提示词、资源顺序和扩展方法见 `assets/art/ART_DIRECTION.md`。
- `python art_checklist.py`：5 项美术回归；加 `--preview` 生成 `docs/art_*_v3.png`。本次另验证了实际 Pet 的六品种构造、窗口 mask 与装备面板。
- 本次美术 5/5、装备 UI 6/6、临时副本战斗 23/23 通过。隔离目录必须先确认存档可写；持久化模块吞掉写异常时会造成断点测试假失败。

- 公式/引擎复现/150 场随机战/节奏/玩法特性/克制/超时平局/竞技场离屏渲染
- **副本挂机模拟器**（无动画快进跑全程）：输出 10 难度通关时长/重试/最终等级 —— 调数值后必看这张表
- 舞台级回归测试：战败复活重建、断点血量保留（这类**表现层状态**bug 模拟器发现不了，必须舞台实测）
- 注意：全量约 5 分钟；绝境/真神模拟 cap 6000 场（长尾墙已证明，别调回去）

## 6. 已知坑清单（每条都真实踩过）

1. import 遗漏导致"点了没反应"（pythonw 无控制台）—— **先看 `error.log`**，excepthook 全程记录
2. mask 外的子部件不渲染；show() 连带显示子部件
3. QSS 不支持 8 位 hex；QScrollArea 视口浅灰底
4. 滚动区内 deleteLater+重建部件有渲染时序坑 → 用"部件池复用"模式
5. `生成数值表.py` 会覆盖手改的 Excel —— 默认不跑
6. UI 渲染问题用**离屏渲染 + 像素采样**验证（`QT_QPA_PLATFORM=offscreen`），截图肉眼不可靠
7. 新增 UI 文件交付前先离屏跑一次构造（两次 import 事故后立此规矩）

## 7. 设计决策记录（拍板过的，别推翻）

| 决策 | 内容 |
|---|---|
| 副本形态 | 直接在桌面上打，不开窗口（用户强烈偏好） |
| 失败惩罚 | 退上一层重来（新手第 1 层除外），无其他损失 |
| 经济系统 | 不做（押注/金币后置） |
| 套装效果 | 后置，表结构已预留 |
| 品质命名 | 普通/悍勇/铁甲/纯银/炫金/无畏/先锋（用户确认） |
| 成长上限 | 80 级（数值表行数决定）；绝望/真神依赖场外经验 |
| 挂机 vs 战斗 | 血量/断点跨模式保留 |

## 8. 后续路线候选（未开工）

音效（战斗全哑巴，最大缺口）→ 好友切磋+回放（种子机制已就绪）→ 押注战绩 → PyInstaller 打包 → Steam。

## 9. 快速上手三步

1. `python main.py` —— 右键看菜单，点【副本】看桌面战斗，点【属性】看装备面板
2. `python battle_checklist.py` —— 23 项全过才算健康
3. 改数值：改 Excel → 重启生效；改完跑 checklist + 看模拟时长表

### 美术修正：死亡条块与腿部
- 已移除 KO 时的七块矩形腹甲及覆盖身体的深色椭圆；手绘角色保持甲壳贴图，以翻转、收腿表现死亡。
- 腿部改为低饱和甲褐色、渐尖胫节/跗节和细描边；棘刺沿胫节分布。
- 新增六品种 KO 身体像素保留回归，美术 6/6、隔离战斗自检 23/23 通过。

- 触须脱离修正：侧视触须根部改为按各品种贴图头壳 alpha 轮廓定位并埋入头部；根部随躯干变换，仅须尖摆动。六品种根部不透明像素回归已加入 art_checklist.py。

### 品种持久化修正
- save.json 的 species_id 为角色品种依据；启动后同步旧 settings.json，设置面板直接读取 pet.species_id。无品种字段的旧存档兼容设置值。
- Pet.set_species 统一重算、同步设置、立即保存角色、刷新选项；信号阻断避免刷新递归触发换种。
- python species_checklist.py：4 项临时存档回归，覆盖冲突设置、切换/重启、旧存档、程序切换。

### 2026-09-24 全项目检查与玩法模拟
- 统一入口 `python check_project.py`：语法检查及战斗 23、玩法 23、品种 4、装备界面 6、美术 7 项，共 63 项；日志 `docs/validation_results.txt`。
- 修复明细、十难度模拟和验证边界见 `docs/CODE_AUDIT.md`，不要沿用历史深渊 111 分钟数据。
- `persistence.write_json` 原子替换单个 JSON 文件并抛出错误；`test_support.isolated_game_data` 供回归、预览使用，禁止测试写玩家真实进度。
- 副本断点保存敌人名单与 RNG 状态；舞台待结算事件只能消费一次，退出时先结算再保存，最终首领同样掉落。
- 条件天赋由 Fighter.effective 按当前资源判定；韧性降低来袭暴击；反伤致死不再继续攻击，end 必须是终局最后事件。
- Pet._shutdown 统一处理退出；旧通关回调不得关闭后来新建舞台；战斗中缩放延迟到挂机应用。
- StatsDB.reload 失败保留有效数据，旧 JSON 兜底保留导出错误；等级不得超过成长表。
- 本轮未修改 Excel、未运行生成数值表.py、未执行提交或推送。离屏测试不替代多屏与系统钩子的实机长期验收。

### 重置语义补充
- Pet._reset 同时通过 DungeonRun.reset_progress 清空断点与通关记录，解锁随之重置。重置前先 BattleStage.discard，再关闭战局；不得调用普通退出结算后再重置，以免产生旧战局掉落。
- 品种、背包装备、位置和偏好保留；旧难度面板销毁，下次重新读档创建。
- 新增两项回归后，完整检查为 65 项（玩法 25 项）。

### UI 墨金主题与副本操作布局
- `ui_theme.py` 统一墨绿底、暖金线框、按钮状态、卡片渐变与矢量盾牌图标；保留血耐、品质色的语义。
- 桌面副本进度与装备/桌面按钮共用 `dungeon_hud_rect` 几何，避免独立坐标漂移。挂机入口与菜单统一名为装备。
- `apply_zoom` 先布局按钮再更新 mask，确保新按钮位置可点击；60%~200% 下按钮均在窗口内。
- `python preview_ui.py` 在临时存档下生成 `docs/ui_*.png`，覆盖挂机、战斗、装备、设置与难度面板，并校验按钮边界与遮罩。

### 入场朝向修正
- BattleStage.update 入场/休整帧不运行 _choreo 和 _separate；沿路径计算 hd 与 thd。进入战斗的最后一帧也不叠加移动。
- _spawn_enemy 清理敌人 KO、逃跑、冲刺、击退及特效状态，防止旧尸体动作污染增援。
- 新增逐帧方向和增援回归，玩法 27 项，合计 67 项。

### 清层提示与装备掉落表现
- DungeonRun.on_fight_end 的 floor_clear 返回 cleared_floor 与 floor，分别表示完成层及下一层；界面据此提示休整。
- loot_effects.py 只负责视觉快照队列；装备实际入库后触发，绝不能在动画完成时再次发奖。复用装备图标与品质色，图标弹起后飞入奖励卡，4.6 秒后过期。重置 discard 会清空队列。
- 玩法回归现为 29 项，完整检查合计 69 项。

## 版本交付要求（用户明确要求）

每次修改完成后，必须完成相关验证、整理版本说明、创建版本提交及递增的版本标签，并推送提交和该标签到 origin。不要只留下本地未提交改动。玩家存档与偏好不得随代码发布；不强制推送、不覆盖旧标签。若推送失败，明确说明阻塞原因。

本次汇总发布 v0.6.5，包含装备框架、美术资源、墨金 UI、玩法与持久化修复、重置副本、进场朝向及掉落表现；69 项自动检查通过。

### v0.6.6：自动连续副本与精简经验显示
- 用户已改定通关流程：5 秒庆祝后自动进入下一难度第 1 层，最后难度才回挂机。回调必须校验原舞台身份及退出状态，避免用户撤出/重置后重开。此规则取代历史“通关自动撤回挂机”。
- 桌面 _draw_bar 仅画经验条和 Lv 等级，字体随尺寸适配；具体升级经验在装备面板 lbl_exp 显示，ViewModel 快照包含 xp 保证实时更新。
- 完整检查为 71 项（玩法 31 项）。
